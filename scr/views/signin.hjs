<!DOCTYPE html>

<html lang="zh-TW">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{title}}</title>

<!-- Bootstrap CSS -->
<link rel="stylesheet" href="/bootstrap.min.css">
<link rel="stylesheet" href="/bootstrap-icons.min.css">
<link rel="stylesheet" href="/css/app.css">


<!-- HTMX & Bootstrap JS -->
<script src="/htmx.min.js"></script>
<script src="/bootstrap.bundle.min.js"></script>


</head>

<body class="container">

<!-- 頁首：專案標題 -->
<header class="text-center my-4">
    <h1 class="text-primary">全球 MMR 可視化與管理系統</h1>
    <p class="text-muted mb-0">Global Maternal Mortality Ratio Visualization & Management</p>
</header>

<!-- 登入頁通常不需要 navbar，如果之後要加保留這一行 -->


<div id="toast" class="fixed top-0 right-0 p-3" style="z-index: 100;"></div>

<!-- 主內容：置中登入卡片 -->
<main id="main" class="my-4">
    <div class="d-flex align-items-center justify-content-center" style="min-height: 60vh;">
        <div class="card shadow-sm" style="width: 360px;">
            <div class="card-body text-center">

                <!-- Logo / Title -->
                <div class="mb-3">
                    <i class="bi bi-bar-chart-line" style="font-size: 2.5rem; color:#0d6efd;"></i>
                    <h3 class="mt-2 mb-0" style="font-weight:600;">MMR Dashboard</h3>                        
                </div>

                <!-- 錯誤訊息（若有） -->
                {{#error}}
                <div class="alert alert-danger py-2">
                    {{error}}
                </div>
                {{/error}}

                <!-- 登入表單 -->
                <!-- 修正 action: 指向 /signin POST 路由 -->
                <form method="post" action="/signin" class="text-start"> 
                    <div class="mb-3">
                        <label for="email" class="form-label">Email Address</label>
                        <input type="email" id="email" name="email" class="form-control" required>
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" id="password" name="password" class="form-control" required>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-box-arrow-in-right me-1"></i> Sign In
                    </button>
                </form>

                <div class="mt-3">
                    <small class="text-muted">
                        還沒有帳號嗎？
                        <!-- 💡 修正連結: 指向 /signup 路由 (移除 /auth) -->
                        <a href="/signup" class="link-primary">註冊</a> 
                    </small>
                </div>

            </div>
        </div>
    </div>
</main>

<footer class="text-center text-muted mt-5 py-3 border-top">
    <p>&copy; 2025 MMR Visualization Project</p>
</footer>


</body>

</html>